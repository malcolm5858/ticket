export type Ticket = {
  id: string;
  name: string;
  email: string;
  description: string;
  status: string;
};

export type formDataType = {
  name: string;
  email: string;
  description: string;
};
